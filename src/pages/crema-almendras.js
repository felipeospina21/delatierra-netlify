import React from 'react';
import Product from '../components/Products/Product';

const AlmendrasPage = () => {
	const index = 2;
	return <Product key={index} index={index} />;
};

export default AlmendrasPage;
